<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="proyectoversion1.app.abdccassiano.com.proyectoversion1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PanelAdmin" />
        <activity android:name=".RegistroExterno" />
        <activity android:name=".PanelGerente" />
        <activity android:name=".PanelNegocio" />
        <activity android:name=".PanelVendedor" />
        <activity android:name=".PanelUsuario" />
        <activity android:name=".RegisterGerente"></activity>
    </application>

</manifest>